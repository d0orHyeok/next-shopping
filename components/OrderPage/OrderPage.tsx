import styles from './OrderPage.module.css'
import classNames from 'classnames/bind'
const cx = classNames.bind(styles)

const OrderPage = () => {
  return (
    <>
      <div className={cx('wrapper')}>OrderPage</div>
    </>
  )
}

export default OrderPage
